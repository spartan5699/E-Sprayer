<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weatherbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".sugarcane"></activity>
        <activity android:name=".cotton" />
        <activity android:name=".custard" />
        <activity android:name=".pomogranate" />
        <activity android:name=".orange" />
        <activity android:name=".lemon" />
        <activity android:name=".grapes" />
        <activity android:name=".banana" />
        <activity android:name=".prediction" />
        <activity android:name=".forecast" />
        <activity android:name=".Sensor_data" />
        <activity android:name=".login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>